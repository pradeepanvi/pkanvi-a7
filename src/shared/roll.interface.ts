export interface Roll {
    roll: {
        icon:string,
        head:string,
        text:string
    }
}

export interface Rolls {
    rolls: {
        icon:string,
        head:string,
        text:string
    }
}
